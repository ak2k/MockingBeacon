build-*/
build/
CMakeFiles/
__pycache__/
*.pyc
.west/
twister-out*/
tests/host/build/
tests/*/build/
result*
.pre-commit-config.yaml

# Local sbomnix artifacts (default output when run without output flags)
http_cache.sqlite
sbom.cdx.json
sbom.spdx.json
sbom.csv

# Local Claude Code permission allowlist (per-machine, per-user;
# accumulated across sessions in this checkout).
.claude/settings.local.json
