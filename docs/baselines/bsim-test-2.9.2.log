## BabbleSim baseline — NCS 2.9.2 (`main` @ bcfddc9)

### Local result (aarch64-darwin)
`nix build .#bsim-test` cannot run on macOS: BabbleSim derivation requires
`glibc-nolibgcc` which is Linux-only (project CLAUDE.md confirms:
"BabbleSim tests (nix build .#bsim-test) ... Linux, Cachix-cached").

Full local failure captured in: bsim-test-2.9.2-darwin-failure.log

This is a pre-existing environmental constraint, not a regression.

### Remote (Linux CI) result for baseline SHA
- SHA:        bcfddc96c7b108224a809499318f2b7d9ac39536 (Phase 0 base = `main`)
- Workflow:   BabbleSim BLE Tests (`.github/workflows/bsim-test.yml`)
- Conclusion: success
- Run URL:    https://github.com/ak2k/Everytag/actions/runs/24523779258
- Timestamp:  2026-04-16T17:13:15Z

The migration's BabbleSim regression check will be performed via the
`.github/workflows/bsim-test.yml` workflow on each phase PR (Linux CI),
and via Cachix-pulled artifacts where applicable.

### Test coverage (per CLAUDE.md)
- tests/bsim/        BLE adv + MAC verification (nrf52_bsim + nrf54l15bsim)
- tests/bsim_dfu/    MCUmgr SMP echo test (nrf52_bsim)
- tests/zms_persist/ ZMS write/remount/read persistence (native_sim, 25 assertions)
