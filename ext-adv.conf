# Extended advertising — boards with >= 64 KB RAM only.
# nrf52810 (24 KB) overflows by ~2 KB with SDC multirole variant.
CONFIG_BT_EXT_ADV=y
CONFIG_BT_EXT_ADV_MAX_ADV_SET=2
CONFIG_BT_BUF_EVT_DISCARDABLE_SIZE=58
